# 0.1.87：从本地候选到Windows试用验证

本版接续0.1.86，保留原生Windows/PySide6桌面。上一轮只交付本地候选；本轮已接入GitHub分支，并执行Windows设置回归。源码运行与打包运行分别留证，不将两组重复测试相加。

## 已纳入源码

基础连接与高级预算分层；保存隐藏预算不清空；修改连接后不沿用旧测试成功说明。图片发送许可、短文本联通与化学识图质量分开说明。本机检查只检查本地组件、资源和目录，不读取密钥或发送教师材料。

软件资源清单含55项现有软件文件，不含原教材、题库、学生资料或系统字体。打包入口增加显式的`--verify-package`自检模式，使用全新临时状态和合成课堂数据，不影响正常启动，也不复用教师账户。

## Windows源码验证

首次源码运行34805912007：218项通过，0失败、0错误、0跳过；实际受测提交d858e0e7。8个主页面、设置的基础/高级/窄窗口和本机检查已执行。完整报告以Actions artifact中原始JUnit和onboarding-smoke.json为准。

## 独立程序验收范围

打包工作流将实际EXE复制到独立临时目录，移除PYTHONPATH、PYTHONHOME、原工作区配置与开发工具PATH，执行正常主窗口打开与关闭，再用同一EXE执行8页导航、图标、设置、草稿保存和原有生产渲染器的合成DOCX/PPTX输出，并检查LibreOffice→PDF→PDFium页图。

只有整个工作流成功并有package-verification.json，才可宣称本版通过上述打包验收。安装发现不等于转换已通过；Pillow绘制的PPT结构预览也不是PowerPoint真实呈现核验。B04仍待推进。

## 保留边界

合成检查不是用户真实题库、全部Windows设备、全部导出类型或课堂效果验收。未调用真实模型，不重导教材。试用包未签名，未证明SmartScreen信誉；不得建议教师关闭系统防护。没有打包Office或系统字体，实际分页仍依赖本机Office与字体。仍未实现作品归档、备份或跨设备同步。
