# 0.1.95接续核查 · 2026-09-15

上一轮源码1dfe5eaa的Windows运行34926675266有1060项：1055通过、4失败、1错误，后续基准、独立程序与发布未执行。原JUnit继续保留在对应Actions artifact。

接续修订1741591f：

- 延迟首屏加载使卡片在父页面resize之后才创建，窄窗口曾保留横排操作造成溢出。改为加入布局前判断可用宽度，卡片resize也更新操作方向；不缩小字号。空状态来源按钮缩短可见文字，保留完整可访问名称与说明。
- 导入成功/拒绝测试补齐题库页面失效接口，并增加每次退出都清除索引的断言；原有接受且追加成功才跳转的三种情形不变。
- 多图测试改为独立工作目录，避免两个夹具重复创建同一目录；一次来源读取、原图字节和答案按需加载的断言不变。
- 新增结果晚于缩放到达的紧凑排列回归。

当前接续环境本地75项非Qt回归通过，Python语法检查通过；PySide6未能安装，本地不计为原生界面验收。同修订的Windows回归、真实显示、基准、实际PDF和发行EXE通过后才发布。不会使用上轮旧程序替代新版本。

研究补查：B站213278507主页依然未完整加载；可读取同作者在火山引擎的原文，包括批量作业与班级报告（7535311403323031595）。Coze Studio官方仓库确认工作流/插件/API及服务端部署结构；本版只学习业务组织，没有新增Coze部署、收费插件或模型调用。读过公开说明不等于完整看完视频或验证其课堂效果。
