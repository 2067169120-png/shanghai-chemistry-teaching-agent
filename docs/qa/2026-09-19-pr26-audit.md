# 2026-09-19：PR #26 收尾核查与桌面可靠性候选

## 结论与范围

建议关闭 PR #26 的旧目标分支比较，不合并至旧版分支；关闭理由是发行与维护已转移，而不是“代码已进入 main”。本次只给出该 PR 的收尾文案，不自动关闭、改目标分支或合并。Issue #24 继续追踪未完成业务。新修复从当前维护提交分出，仍是候选；不创建 v0.1.102，不覆盖 v0.1.101 EXE，不删除分支或移动标签。

本次源码依据：已下载原发行 delivery artifact，核对清单内8个成品的大小/SHA256；应用源码取自发行 source.zip。GitHub compare 确认发行到维护头的29文件差异仅属文档及其检查逻辑，integrations 应用代码无变化。下列分支位置是审查开始时快照，后续候选提交不改变该历史证据。

## 版本、分支与验证的实际关系

| 对象 | 核对结果 |
| --- | --- |
| PR #26 | open、未合并；head 为 `feature/lesson-source-0.1.101`，base 为 `feature/lesson-design-0.1.100`，不是 main |
| 旧版 base | `24ef3c6eb01e1a95be7c9267bbf4210658ac6e10`；维护头比它领先13次提交、落后0次 |
| 维护头 | `1ce620d4ac3bb2fdbc8cc9f9d9ac6fc73f202e0d`；已包含 PR #27 文档整理 |
| 发布标签 | 附注标签对象 `421797f707315f5fb7a7097184730c728ecdd31d` 解引用到发行提交 `83750196ea1ffbdd116095175a87d5fba4e76b7c` |
| Release | ID 391501432；2026-09-18 13:36:15 UTC 发布，draft=false、prerelease=true，是已发布试用版，不是正式稳定版 |
| 发行受测源码 | `c0ca081357ccea5094ac449381dbbcb38654f47a`；与追加发行说明的提交分开记录 |
| main | `9016ac11b4f2b752dbc86a0c954cbc8011b4f05b`；PR #28 只更新完整 README，不合入应用代码 |
| main 与维护头 | 分叉共同祖先 `0fd71d35077a285920dc9b60a94ee71860d3590a`；以维护头为 base 比较 main，ahead=2、behind=232。不能理解成 main 只落后一个小补丁 |
| 发行后差异 | 维护头领先发行5次提交，29文件为文档/检查范围；不把当前维护头说成当时受测 SHA |

原运行 [35349491417](https://github.com/2067169120-png/shanghai-chemistry-teaching-agent/actions/runs/35349491417) 的 verify 与 publish 均成功，原清单记录1280项 Windows 定向测试、0失败/错误/跳过，并有同一发行 EXE 独立目录验证。它不是本次运行结果，也不是全仓全部测试。`releases/latest` 返回404不能否定预发布存在，须检查 releases 列表或指定 tag。

依据：[PR #26](https://github.com/2067169120-png/shanghai-chemistry-teaching-agent/pull/26)、[PR #27](https://github.com/2067169120-png/shanghai-chemistry-teaching-agent/pull/27)、[PR #28](https://github.com/2067169120-png/shanghai-chemistry-teaching-agent/pull/28)、[v0.1.101](https://github.com/2067169120-png/shanghai-chemistry-teaching-agent/releases/tag/v0.1.101)。

## 代码审查：已修复候选与仍需治理的问题

| 优先级 | 实际发现与影响 | 本轮处理 |
| --- | --- | --- |
| P1 | `desktop_state.py` 将存在但格式错误的 basket/drafts 静默替换为空；之后保存窗口位置等无关动作会覆盖可恢复内容 | 对存在但类型错误的持久数据拒绝读取/保存，保留原字节；缺失的旧可选字段仍兼容默认值。26项回归，旧代码24失败/2通过，新代码26通过 |
| P1 | `explorer_basket.py` 读取前清列表，异常后显示0项；错误与空题篮混淆，组卷入口仍可能可用 | 先完成读取/身份检查再更新视图；失败保留上次显示并明确不代表当前记录，锁住移动/删除/输出，增加重试；输出前重新核对 |
| P2 | `question_explorer_page.py` 添加前/高级窗口退出后直接读取题篮，失败可抛出槽异常；刷新错误时旧加入按钮未禁用 | 统一失败状态、禁止加入/预览、不广播虚假0；成功读取后恢复。沿用原 facade 和题目身份 |
| P2 | 题篮底部三个长按钮固定一行，窄窗容易挤压；删除中间题后焦点跳首项 | 小于560逻辑像素时底部纵排，增加当前行位置，删除后选相邻项；保留原中文字号、绿色主题和完整公共材料 |
| P1治理 | `main` 仅首页指南更新，源码与介绍分离；旧功能 PR 串联多个版本 | 本轮明确关系，不用“mergeable=true”替代发布/回归判断；主线整合另开独立PR |
| P1治理 | 全仓测试收集依赖 PySide6 与仓库外私有材料；干净检出不能直接复现所谓全测试通过 | 保留原失败日志，区分公开合成回归、原生UI与私有资料集；不批量删测或改断言以取得绿色 |
| P2治理 | 发布 workflow 仍绑定固定版本维护分支，push 会触发完整打包发布流程 | 新改动只放新候选分支；验证流程不发布、不改标签，不直接推维护头。后续应拆分验证与显式发行 |
| P2治理 | facade 6948行、service 6380行等大模块使职责与回归面难隔离 | 记录后续拆分边界，先围绕存储/身份/导出契约加测试，不在收尾时大重构 |

332个 integrations/runtime Python/PYW 文件已做语法级检查，未发现解析错误。这不等于332文件逐行人工安全审计，更不等于全部业务正确。重点阅读了存储、题篮、选题入口、初稿转换、备份边界及发布流程。未处理真实学生、私有题库、密钥或模型网络请求。

## 参考现有产品，但不冒充已复刻

[解题策主站](https://www.sjep.net/)公开介绍可核对到自定义题库、LaTeX、错题整理；[其 Typst 入口](https://x.sjep.net/)公开说明基于 LaTeX/Typst 编写课件与试卷。页面为动态应用，本次没有完整读取登录后的工作台，不能据此声称其具体拖拽、批改、同步能力已被验收。

本项目借鉴的是“个人资源可整理、检索后有明确选中状态、编辑与成品预览相衔接”的工作方式，不复制内容或品牌，也不改成 Web 站点。本轮先让已有选题篮的异常、空态、选择位置和窄窗动作可信。

后续 UI 的验收目标保持为：左侧资源树与筛选、中间完整题面/纸面、右侧属性；统一题目副本编辑及来源可追溯；教师/学生输出分离；保存重开、版本过期提醒、备份恢复均通过。它们是待办，不是本轮已经实现的三栏画布。优先继续原 X02 独立复练题集、X03 改分后过期提示、X09 跨业务迁移，不新增一套题库或学生身份。

## 本轮验证口径

- 全仓首次收集：20个错误、18个跳过、4个警告后中断（达到 maxfail=20），没有完成全套执行。错误包含缺少 PySide6、`parallel_outputs/intake_batches_v2`、本机 `sh-chem-db` 脚本/材料等。
- 修改前9个独立核心模块：139通过。新增存储反例回放：旧代码24失败/2通过；修复后26通过，保留失败记录。最终本地选定集合165通过，2个UI模块因缺少PySide6跳过；这些结果互相重叠，不相加。
- 当前容器为 Linux/Python3.13，无 PySide6，没有在本地运行 Windows EXE。原生界面结果来自下述真实 Windows/Python3.12 CI，不借用旧1280项数字。
- 新UI测试覆盖读取失败/重试、真空态、相邻选择、写失败、预览前再读取、无身份条目、窄宽窗口、选题页加入保护，并保存真实Qt截图。没有生成效果图冒充运行截图。
- 本轮不生成新的发行EXE。源码UI CI通过不替代新的打包版、实际Office分页、学校电脑多屏验收。

### 已完成的候选 Windows 验证

[运行35419340939](https://github.com/2067169120-png/shanghai-chemistry-teaching-agent/actions/runs/35419340939) 的应用提交和 Windows verify 均成功。外层工作流启动提交为 `52de789282c9d4cf6a0dd0a59d7f04823cf752ef`；实际检出、编译和测试的源码为 **`8d87661637562b5d225131bf559e4deef32f410d`**，以附件 `candidate-qa/source.json` 为准，不能将二者混写。

**199项通过，0失败、0错误、0跳过，13个定向测试文件，pytest耗时55.267秒。** 包含既有核心、原选题UI、新存储保护、新题篮UI和12项完整README回归。compileall和文档政策检查也通过。它是本次源码定向集合，不是全仓测试，也不与165或历史1280累加。

已重新下载artifact **10576986452**，核对 `pytest.xml` 与 `source.json`，并逐一查看 `basket-wide.png`、`basket-narrow.png`、`basket-recovery.png`。分别为680/420逻辑像素宽度的实际Qt窗口：题号与来源可读、底部动作不横向挤压，错误保留上次行并显示状态待核对及重新读取入口。数据全部为合成材料。

原始artifact ZIP SHA256：`f554770cd44ee5be62d64fe61cdd589cc5de749255425fbe270abafc95569a00`。保留[首次失败运行35419234067](https://github.com/2067169120-png/shanghai-chemistry-teaching-agent/actions/runs/35419234067)：README前缀补丁的上下文不匹配导致应用前停止，没有写入业务改动；修正为唯一精确上下文替换后再成功执行。未降低校验、删测或覆盖失败日志。

临时压缩补丁已在应用提交删除，本次最终树再删除一次性写入工作流，只保留内容只读、不打包发布的 `basket-audit.yml`。之后的文档/清理提交与上述受测应用代码保持一致；最终PR头仍应运行相同检查。合并候选前必须先处理旧维护分支的固定版本自动发布触发，或选择已移除该风险的整合分支，不能直接把源码通过当作再次发布v0.1.101的许可。

## PR #26 状态更新草稿（尚未代发）

建议标题：`0.1.101：初稿转教学环节与三类成品备份，Windows试用版已发布`

> v0.1.101 已于2026-09-18 13:36:15 UTC以预发布试用版发布，原 Windows 定向测试及同一发行 EXE 验证均有记录。原“Windows验收中”状态已过期。受测代码 c0ca081、发行提交8375019、当前维护头1ce620d分别列明；发行后的5次提交为文档及其检查逻辑。PR #27合入维护分支，PR #28仅更新main README，应用源码尚未进入main。本PR目标仍是旧0.1.100分支，不应把合并本PR解释为主线整合完成。Issue #24与后续候选继续独立追踪。

建议关闭说明：

> 本PR原先用于比较0.1.101与旧0.1.100开发分支。相关版本已经从feature/lesson-source-0.1.101发布并继续维护，故关闭这一旧目标分支的比较PR，不再反向合并至0.1.100分支。关闭不表示代码已合入main，也不删除维护分支、既有提交、版本标签或发行附件。main目前仅同步发布指南；主线应用整合须另开PR审查。Issue #24不随本PR关闭。

若继续保留历史发布比较PR，应使用上述已发布标题和最新说明，并明确它只作历史比较；不能继续用“Windows验收中”表达未知状态。
