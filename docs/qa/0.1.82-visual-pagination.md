# 0.1.82 统一题篮与真实分页验证

本版验证范围是原生桌面的个人图片整主题入篮、混合编排、预览和导出，不表示全库已导入、化学答案已审定或课堂课件已验收。

## 实际样例

使用已归档银镜主题的9道印刷题和20分来源参考答案，只读来源，题篮、分页和导出写入独立QA状态目录。没有调用模型或改写用户模型配置。

首轮实际渲染为学生2页、教师4页。逐页查看发现短图被全宽放大、第2题题干与选项跨页、无小问标签显示成None及摘要重复。首轮结果保留用于排查，没有作为通过结果。

修复后用新目录重新生成，学生2页、教师3页。5页全部查看：第2题题干和选项同页，短答案按原页像素比例呈现，None标签消失；题图、共同材料和教师答案图可见，无页面裁断。学生版不带教师答案图。原图本身的清晰度、原印刷分数和答题区域保持不变，不代表所有来源图已重裁。

软件使用本机Word COM转换PDF，再由现有render_docx.py路径生成300dpi PNG；这是应用实际转换验证，不宣称通过独立文档技能的标准LibreOffice流水线。当前会话没有可调用的load_workspace_dependencies工具。另在隔离状态中模拟分页确认，验证学生/教师DOCX及PDF共4个文件与冻结预览逐字节、SHA-256相同，且导出没有重新构建或渲染。模型查看和隔离确认不是教师批准。

本机证据在`runtime/deeptutor_shchem/qa/visual-mixed-pagination-20260914-r1`与`r2`；真实题目与过程文件不纳入公开仓库。真实用户状态在两次渲染及导出检查前后均一致。

## 自动化检查

统一使用项目专用Python、UTF-8、pytest禁用缓存；Qt界面为offscreen。下列组分别运行，不相加宣称全项目测试覆盖：

| 范围 | 结果 |
| --- | --- |
| mixed service、mixed export、分页及visual writer | 109通过 |
| visual theme export及完整入篮到四文件导出 | 39通过 |
| personal visual、整主题入篮、mixed新旧UI | 83通过 |
| visual provider预算、Word输出预算、mixed入口 | 150通过，3项已有字体API弃用警告 |
| 模型限额界面 | 27通过 |

覆盖冻结文件、答案隔离、来源/CAS/裁片变化、分页缺失/损坏/伪PDF、取消、旧确认、重复分页，以及未知分值不充当完整总分。分页器的合成测试不算真实Office渲染；Qt截图不算真人原生EXE操作验收。

Ruff采用项目已使用的`staging/third_party/DeepTutor/pyproject.toml`配置；不使用机器全局的额外规则来改写无关旧代码。发布前另核对Git差异和构建模块。

提交范围检查对本轮28个变更/新增文件未发现敏感值或不允许的资料类型。旧全量审计脚本的截图白名单滞后，对已提交的历史合成图片报告类型问题，因此未把该全量运行记作通过；本轮新截图已单独目视及PNG检查，不扩大对真实资料图片的许可。

## 桌面构建

构建标签`0.1.82-visual-pagination`，已有构建未覆盖。EXE为8,888,234字节，SHA-256为`d3f68de4c9c9f9f05ee3aaafa01445513a4d274580b4f455c749698c9b871838`。13个相关模块的冻结代码与工作区源码一致，包括新增分页器、图片主题导出与writer、统一服务及UI；没有启动EXE，不冒充真人操作验收。根VBS首选本版、保留历史回退；启动器测试2通过、1项另一分发包路径检查跳过。Ruff与Git差异检查通过。

## 界面与待办

README使用的分页界面图完全来自合成fixture，无真实题目或个人资料。生成脚本为`staging/coordination/deeptutor_gateway/scripts/capture_visual_pagination_ui_demo.py`。首轮离屏字体异常图保留在私有QA目录，公开图使用修正后的r2。

仍需继续验证更多跨来源、跨主题和超长综合题页面；老师打开同一DOCX后手动编辑会改变排版，不能继续把它视为原预览。全库标记、剩余公众号题目、教材/教案深度蒸馏、真实用户新模型表现与课堂效果均未由本轮测试完成。
