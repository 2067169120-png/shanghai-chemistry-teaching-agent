# 0.1.98 发布后复核

## 交付身份

- 实际受测/打包源码：`8d86f33a615b2e58fefe5a6682c4c1a1c2e6dfd1`。
- 发行提交及`v0.1.98`标签：`989e59fcd41881de5801d17533b4d74f5311e7bb`。
- Windows验证运行：35248033607，verify成功，1195项定向测试全部通过，0失败、0错误、0跳过；同一EXE在独立目录完成实际考试面板流程。
- 原publish因GitHub上传源码附件返回HTTP500失败，保留原失败记录。不是应用测试失败，也不重写成原工作流整体成功。
- 恢复发布运行35256599710成功；仅补齐原交付附件并发布原草稿，未重新打包、未移动版本标签、未合并main。

## 上传恢复经过

第一次恢复运行35256019559过度假定失败附件starter的size必为0，因此停止，未发布。实际源码附件570683644状态为starter、digest为空、size为请求声明的86745800字节。后续记录这个未完成附件元数据，清理其占位，再上传原校验通过的源码ZIP；已完成且匹配的附件没有替换。

GitHub把中文成绩示例文件名规范化后，文件名与原清单不同。保留原始RELEASE-MANIFEST.json和SHA256SUMS.txt字节；示例下载名改为`ShanghaiChem-0.1.98-Score-Import-Example.xlsx`，内容不变。另附`RELEASE-DOWNLOAD-NAMES.json`与`SHA256SUMS-Release.txt`用于实际下载名核对。恢复工作流下载并核对全部10个最终附件的字节、大小和SHA256，确认后才取消草稿状态。

## 当前接续独立检查

再次从原Actions交付包提取全部成品：原校验清单7项通过，发行清单各附件大小/哈希一致。便携包内BUILD对应受测提交，EXE与Windows报告哈希相同：

`7c72a5fc9db2895577817826a6b3497cb01e86ed425fcce6fa7e3770a2eb1704`

便携ZIP SHA256：

`694dc44bbba60a9c6b5ce086962342f58c6f577571423628b1dad7b77537c409`

程序及源码ZIP未包含字体文件。递归171条截图记录均匹配QA里的文件哈希（含重复记录，不等于171种独立场景）。实际查看本版Excel题号映射、总览、小题、学生热力表、API及800×700窗口，使用的均是合成验收数据，不是真实学生或真实课堂效果。

README有346行、16个目录锚点、24张不同操作图，八主页面、导入、模型设置、备份和旧业务仍完整介绍；图片路径、HTML全部内嵌图和目录跳转通过。成绩示例含“成绩／题目映射／使用说明”三张表。本轮没有改写已发布README或模板字节。

当前Linux另运行test_exam_analysis.py与test_complete_readme.py：32项通过，0失败/错误/跳过。该集合与Windows1195项重叠，不相加。本地没有运行Windows EXE，也没有把Qt offscreen的截图说成全Windows缩放验收。

## 教师可用范围

入口为学生分析→考试分析（Excel），或Ctrl+K搜索。xlsx显式列映射和教师确认满分；总览、分班、逐题、主知识点、逐人热力表与数据核对由本地统计生成。缺考/缺失不当0，只有总分不生成逐题结论。真实试卷可用DOCX/PDF/PNG/JPEG/BMP读入，公共材料需保留与核对。

API沿用已配置服务商与明确发送确认；默认只发去姓名、班级名的汇总，匿名逐人及图片可选。模型只返回讲评、分层行动与复测草稿，不执行其代码、不改统计、不虚构题库ID。自由文字/试卷图片仍由教师检查个人信息；没有足够数据不能断言学生粗心或能力不足。调用链以固定本地模拟传输验证，网络请求0，未用真实模型或学生资料验证。

历史分析可保存、重开、导出离线HTML并带入备课材料。快照不在旧备课ZIP中。Excel学生编号还未对齐既有学生档案；这不是跨考试进步追踪，也不是作业批次自动汇总或全题库复练闭环。成绩与材料不足时说明限制，不承诺提分。

## 下一步

优先让教师确认Excel学生与本机档案/作答的关联、考试题目与题库身份的关联，再从已核对失分点选真实完整题作复练；跨考试趋势需要身份与分值口径明确，不直接把不同难度试卷均分差称为提升。原批次紧凑布局、学生/考试备份、全量题库迁移与实际PPTX核验仍按任务表分批推进。
