# 0.1.97发布后复核 · 2026-09-17

## 交付身份

- 已发布标签：`v0.1.97`；PR #21保持开放，未合并main。
- 实际受测/打包源码：`5a5d8f59eefc62bbab6c3035d49ff4e7b41659e2`。
- 标签对应发行提交：`2611c48eadc0913d8a1b37f0d39a5aa3231b419a`。
- Windows运行：35231209253，测试、源码操作、独立EXE及发布已通过。
- 便携ZIP SHA256：`0c879f00e03715db4f7188b33aeed7785437a82858bcd2c7b78e0f2a6851c594`。
- EXE SHA256：`1c41da3c688320d5a23d32aa1f4818bb53a9177dae2c7ca2c19f701aed391951`，与Windows程序报告一致。

本文件为发布后的核查记录，不修改程序、旧标签或Release附件，也不产生一个假定重新受测的源码版本。

## 重新下载核对

已重新下载发行产物，解出便携包、源码、Windows证据、HTML指南、发行清单和校验表。SHA256SUMS中全部五个文件逐一通过；发行清单各附件的大小与哈希对应。BUILD.json指向受测提交，ZIP内EXE与验收EXE哈希一致。程序和源码ZIP均未包含字体文件。

源码与独立程序机器报告中的159条递归截图记录均在QA包中找到并核对哈希；包含重复记录，不能当作159种独立测试场景。本版六张作业批次截图与源码中的同名图片字节一致。实际查看了选择成员、宽窗口和800×700紧凑窗口，未将示意图当作软件截图。

## 测试范围

原始JUnit、发行清单和机器报告一致：**1154项Windows定向测试，0失败、0错误、0跳过**，包含既有业务，不是1154项全针对作业批次。源码和同一发行EXE都走过实际入口、明确勾选已有作答、跨学生切换、输入暂存与重开、正式评分、情况记录、原图/模型稿不变检查。

当前Linux容器另跑`test_work_batches.py`与`test_complete_readme.py`：**22项通过，0失败/错误/跳过**。这22项是上述Windows集合的子集，不相加。当前环境无PySide6，没有在此执行Qt界面或Windows EXE；未重复声明当前机器完成Windows验收。

批次与同屏自动化使用Windows上的Qt offscreen后端；字体倍率另查Windows后端。两个匿名合成学生和固定本地返回稿不证明真实识别、评分准确率或课堂效果。无真实学生、教材处理或网络模型请求。

## README完整性

发行README共287行，含18张不同操作图、15个手动目录锚点、四条日常工作流程。八个主页面及作业批次、导入/教材标签、设置、中文显示、备份范围、安装升级、常见问题和版本入口均保留，不需要读旧版本才能了解现有操作。

所有图片路径存在，15个页内导航有效；HTML指南内嵌图片与目录目标已核对。旧0.1.96 README归档与该版发行源文件原字节一致。仓库已有两个README回归用例，后续仍须保留完整功能介绍，而不是用更新日志替代README；实际功能是否可用另由业务回归和界面检查验证。

## 实际发现的后续界面问题

**800×700布局的操作按钮虽然可达，但批次/学生/题目导航和多段重复状态说明占用了较多竖向空间，原作答的可视区域仍偏小。** 下一批应合并重复状态、收起不常用说明、把情况记录做成可展开的紧凑区，给原图和评分更多高度。不能以几何断言通过代替持续批改效率。

目前没有修改这一已发布程序的布局。后续修正必须使用新版本、对应测试与实际截图，不改写0.1.97的验收结果。

## 未完成范围

作业批次只组织已有作答，并非批量导入/循环调用模型。情况记录不自动改分；未评分不按0分。暂存发生在主动点击、切换或返回时，不是每键自动保存，异常退出前最新未暂存内容仍可能丢失。学生作答、批次、状态和暂存尚未纳入备课ZIP。

班级汇总与有效样本分母、逐评分点修改、真实题库复练/复测、学生资料备份仍在Issue #19；原Word/公众号完整迁移在#9，复杂图号/引用在#14。教学节点编辑与实际PPTX视觉兼容继续保留，不能因为本版已发而默认完成。
