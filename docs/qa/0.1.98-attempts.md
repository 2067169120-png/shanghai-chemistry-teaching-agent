# 0.1.98验收与阅读改进记录

首轮d640e6f5的实际Excel映射、五个面板、按班筛选、Word材料、固定本地API传输与保存/导出探针通过。样例为12条合成记录、10个有效总分、均分69、中位数72。网络请求为0；固定返回稿不作为真实模型教学质量证据。

查看首轮截图后，次要按钮与主按钮同为大面积填色，宽窗也占两行，班级下拉占去整行而考试名称窄小；API结果在紧凑窗中缺乏稳定阅读高度。因此在76016371中把次要操作改为轻按钮，宽窗工具条单行、窄窗自动两行，限制班级选择框宽度，API页可以整体滚动且结果保留至少170像素高度。新增实际800×700滚动到结果后的可见面积检查，不仅检查按钮存在。

本地新增视觉API正向测试，核对明确声明视觉能力时发送的图片字节与确认副本一致；无视觉能力路径仍在读取凭据前提示。核心/指南本地32项通过，Qt本地未安装，11项新界面用例交给Windows。最终数量只取一次完整Windows运行，不把局部或历史版本重复相加。

布局补丁第一次因传输时误录了三处上下文/行数，完整SHA256不符而未应用；修正传输文本后与原本地已测补丁哈希一致再执行。没有降低检查条件或将首次传输失败称为软件回归失败。临时传输文件与工作流已移除，最终只保留功能代码、用例和正式版本工作流。

当前提交重新执行源码实际入口、全部既有定向回归与新增用例、字体/选题/组卷/批改/备份等原流程，以及同一发行EXE。只有本次提交完整通过才建立v0.1.98及Release。完整README继续覆盖所有主页面和功能，不以本文件代替使用指南。
