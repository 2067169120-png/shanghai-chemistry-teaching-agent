# 0.1.93 实际尝试记录

## 字体先行检查

提交b674a677 / 运行34912130654：14项字体用例通过，Windows原生Qt后端1/1.25/1.5倍率样例均无汉字或化学符号缺字。此为早期子集，不与后续完整回归数量相加。

## 扫描图端到端初轮

提交4aaf71a2 / 运行34913411971：41项字体、像素和原生交互测试通过，但实际DOCX转PDF失败。用相同PNG、JPEG文档在本地配对复现后，发现图片类型清单改成前缀命名空间影响LibreOffice读取；改为保留原OPC默认命名空间，两种文档均实际转换成功，另增2项命名空间用例。原测试不删，失败历史保留。

## 全回归发现的兼容问题

提交3ddd0d70 / 运行34914577821：966项中964通过、2失败。旧题库资料带入备课的两个用例依赖原生组卷页的_composer，暴露了新增页面绕过旧滚动外壳的问题。修复为保留原WorkflowPaperPage外壳、转发接口和滚动，仅将内部组卷器扩展为带换号入口的版本。未修改或放宽这两个旧用例。

检查实际截图后，将取消、移除、撤销、清除降为次要按钮；字体样例窗口使用真实不透明页面底色，避免透明PNG在暗色阅读环境中难以辨认。没有后期改写截图或用概念图替换运行图片。

最终受测提交、计数、原生源码/EXE流程和发布结果以本版readiness/package报告为准。所有计数是重叠集合，不累加。未调用真实模型、读取用户资料或分发字体文件。
