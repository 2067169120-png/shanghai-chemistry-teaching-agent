# 0.1.95验证范围与尝试记录

新增可丢弃PersonalSearchIndex、PersonalCatalogSession；复用全部有效标签判定，200组固定随机组合与原实现比较完整结果。索引4组LRU只缓存匹配位置；原题选用与导出不依赖其真实性。显式重新读取及导入/标签管理失效，外部文件修改仍须重新读取。取消旧请求不再发布结果，已开始的来源读取可能先完成后由最新请求复用。

Word图片合并读取沿用原修订/资产归属、容器和格式检查，同标签页一次解析；原图字节不降质、不跨会话当成权威缓存，失败保留提示。仍在主线程建立QPixmap，超大图进一步解码优化未完成。

记录首次建立索引与后续查询，计时不包含全屏绘制。源码和发行EXE探针使用16道合成题、首题8图；检查首次一次来源解析、翻页复用、最后条件生效、复制诊断无题干/关键词、保存来源、导入失效与备份独立恢复。原业务回归沿用之前模块。

本地首次84项集合78通过/6失败：检查旧备份时不应添加不存在的新统计字段，否则清单身份发生变化。已改为仅在字段存在时规范化新统计，旧ZIP不增字段；原测试未删除。随后同集合84通过。旧导入按钮的文字断言改为新“旧版资料包”措辞，保留旧包操作和关闭保护；没有删业务断言。当前本地没有PySide6，不能把本地结果说成Windows界面通过。

新题号区域备份是坐标/哈希扩展，不存图文和号码；字段异常不悄悄漏掉。新备份不保证旧0.1.94可读取，但新版本能读无扩展的旧ZIP。原题库、学生业务和模型密钥仍不打包。

最终计数、源码身份、Windows与独立EXE探针见机器报告。模型调用为0，合成截图非真实教师资料。作者案例仅按公开原文阅读，详见research，不宣称看完全部视频或已经合并第三方工程。
