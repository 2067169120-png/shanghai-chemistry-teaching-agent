# 原生控件标识

这些SVG是项目原创的下拉箭头、数字增减和勾选标识，用于避免不同Windows主题与QSS叠加时标识缺失；不含第三方商标或字体。

源码运行直接读取相邻SVG。构建时将本目录作为数据文件复制到`integrations/deeptutor_shchem_v1/desktop_workbench/studio_assets`；根构建脚本已列出此目录。当前0.1.84仍为源码版本，本轮未执行独立EXE构建。
